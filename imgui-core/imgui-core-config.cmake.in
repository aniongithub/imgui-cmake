if (NOT TARGET imgui-core)
    include(${CMAKE_CURRENT_LIST_DIR}/imgui-core-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/imgui-core-targets.cmake)
endif()

check_required_components(imgui-core)