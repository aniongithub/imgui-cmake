if (NOT TARGET imgui-glfw)
    include(${CMAKE_CURRENT_LIST_DIR}/impl-glfw-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/impl-glfw-targets.cmake)
endif()

check_required_components(imgui-glfw)