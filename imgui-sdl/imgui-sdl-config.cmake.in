if (NOT TARGET imgui-sdl)
    include(${CMAKE_CURRENT_LIST_DIR}/impl-sdl-config-version.cmake)
    include(${CMAKE_CURRENT_LIST_DIR}/impl-sdl-targets.cmake)
endif()

check_required_components(imgui-sdl)